# =============================================================================
# CINECHATTER ENVIRONMENT VARIABLES
# =============================================================================
#
# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual keys
# 3. Never commit .env to Git (it's in .gitignore)
#
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (Database & Authentication)
# -----------------------------------------------------------------------------
# Get these from: https://supabase.com/dashboard → Your Project → Settings → API
VITE_SUPABASE_URL=your-supabase-url-here
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# -----------------------------------------------------------------------------
# Anthropic Claude API (AI Article Generation)
# -----------------------------------------------------------------------------
# Get your API key from: https://console.anthropic.com/settings/keys
# NOTE: For DEVELOPMENT, use VITE_ prefix (exposed to browser)
# For PRODUCTION (Vercel/Netlify), set ANTHROPIC_API_KEY (server-side only)
VITE_ANTHROPIC_API_KEY=sk-ant-api03-your-api-key-here

# -----------------------------------------------------------------------------
# OpenAI API (ChatGPT Article Generation)
# -----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
# NOTE: For DEVELOPMENT, use VITE_ prefix (exposed to browser)
# For PRODUCTION (Vercel/Netlify), set OPENAI_API_KEY (server-side only)
VITE_OPENAI_API_KEY=sk-your-openai-api-key-here

# -----------------------------------------------------------------------------
# Google Custom Search API (Web Search for AI Articles)
# -----------------------------------------------------------------------------
# API Key: Get from https://console.cloud.google.com/apis/credentials
# Search Engine ID: Get from https://programmablesearchengine.google.com
# Free tier: 100 queries/day
VITE_GOOGLE_SEARCH_API_KEY=your-google-search-api-key-here
VITE_GOOGLE_CSE_ID=your-custom-search-engine-id-here

# =============================================================================
# PRODUCTION DEPLOYMENT (Vercel/Netlify)
# =============================================================================
#
# Set these environment variables in your hosting platform dashboard:
#
# Required for Serverless API Routes:
# - ANTHROPIC_API_KEY=sk-ant-api03-...  (WITHOUT VITE_ prefix)
# - OPENAI_API_KEY=sk-proj-...          (WITHOUT VITE_ prefix)
#
# Required for Client-Side:
# - VITE_SUPABASE_URL=https://xxxxx.supabase.co
# - VITE_SUPABASE_ANON_KEY=eyJhbGci...
# - VITE_GOOGLE_SEARCH_API_KEY=AIzaSy...    (if using Google Search)
# - VITE_GOOGLE_CSE_ID=your-cse-id           (if using Google Search)
#
# Security Notes:
# - DO NOT set VITE_ANTHROPIC_API_KEY or VITE_OPENAI_API_KEY in production
#   (these would expose your keys in the browser)
# - The app automatically uses serverless API routes in production
# - Google Search credentials (VITE_ prefix) are safe to expose as they're
#   domain-restricted and rate-limited
#
# =============================================================================
